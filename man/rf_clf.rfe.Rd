% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranger_RF_util.R
\name{rf_clf.rfe}
\alias{rf_clf.rfe}
\title{rf_clf.rfe}
\usage{
rf_clf.rfe(
  x,
  y,
  nfolds = 5,
  n_features = NULL,
  metric = "AUROC",
  positive_class = NA,
  tolerance = 0.01,
  recursive = TRUE,
  ntree = 500,
  verbose = FALSE,
  rf_model = NULL,
  ...
)
}
\arguments{
\item{x}{The data frame or data matrix for model training.}

\item{y}{A factor related to the responsive vector for training data.}

\item{nfolds}{The number of folds in the cross-validation, or a factor defining customized folds (e.g., leave-one-group-out).}

\item{n_features}{A numeric vector of feature set sizes. By default powers of two up to 1024.}

\item{metric}{The classification performance metric used for selection: one of "AUROC", "AUPRC", "Accuracy", "Kappa", "F1".}

\item{positive_class}{A class of the y.}

\item{tolerance}{The relative tolerance: the optimal parsimonious model is the smallest feature set
whose performance is within \code{tolerance} (default 1\%) of the best performance.}

\item{recursive}{A boolean value indicating if the feature ranking is re-computed at each step.}

\item{ntree}{The number of trees.}

\item{verbose}{A boolean value indicating if showing computation status.}

\item{rf_model}{An optional pre-computed \code{rf.cross.validation} or \code{rf.out.of.bag} model on all features.}

\item{...}{Other parameters applicable to \code{ranger}.}
}
\value{
An object of class \code{rf_clf.rfe} including the performance table (\code{top_n_perf}),
the best and optimal number of features, the \code{selected_features}, and the models at each step (\code{top_n_rf}).
}
\description{
Recursive feature elimination (RFE) for random forest classification.
RF models are built with cross-validation on successively reduced feature sets
(by default the top 2, 4, 8, 16, 32, 64, 128, 256, 512 and 1024 features by importance, plus all features).
With \code{recursive=TRUE}, the importance ranking is re-computed from the model at each step;
otherwise the ranking of the model with all features is reused.
The same cross-validation folds are used at every step, so that feature sets are compared on identical splits.
}
\details{
As the features are ranked using all samples, the cross-validated performance of the selected
feature sets is optimistic. The generalizability of the selected biomarkers should be assessed in
independent datasets, e.g., using \code{rf_clf.cross_appl} or \code{rf_clf.lodo}.
}
\examples{
set.seed(123)
n_features <- 100
prob_vec <- gtools::rdirichlet(5, sample(n_features))
x <- data.frame(rbind(t(rmultinom(7, 7*n_features, prob_vec[1, ])),
            t(rmultinom(8, 8*n_features, prob_vec[2, ])),
            t(rmultinom(15, 15*n_features, prob_vec[3, ])),
            t(rmultinom(15, 15*n_features, prob_vec[4, ])),
            t(rmultinom(15, 15*n_features, prob_vec[5, ]))))
y<-factor(c(rep("A", 30), rep("C", 30)))
rfe <- rf_clf.rfe(x, y, nfolds=5, metric="AUROC", ntree=200)
rfe$top_n_perf
rfe$selected_features
}
\seealso{
plot_clf_feature_selection plot_topN_imp_scores id_robust_markers
}
