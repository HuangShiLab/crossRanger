% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranger_crossRF_plot_util.R
\name{id_robust_markers}
\alias{id_robust_markers}
\title{id_robust_markers}
\usage{
id_robust_markers(rf_features, bg_test, top_n = 20, q_cutoff = NULL)
}
\arguments{
\item{rf_features}{The features selected by random forests: the result of \code{rf_clf.rfe}/\code{rf_reg.rfe}
(its selected features), a model from \code{rf.out.of.bag}/\code{rf.cross.validation} (its \code{top_n} features),
or a character vector of feature IDs.}

\item{bg_test}{The output data.frame of \code{BetweenGroup.test} (rownames are feature IDs).}

\item{top_n}{The number of top-ranked features used when \code{rf_features} is a model.}

\item{q_cutoff}{An optional cutoff of adjusted p values of the non-parametric test. By default, the enrichment
(\code{Enr}) of \code{BetweenGroup.test} defines the significant features.}
}
\value{
A data.frame of all features with their RF importance rank (if available), the adjusted p value,
log2 fold change, enrichment, and whether they are robust markers (selected by RF and statistically significant).
}
\description{
Intersect the machine-learning-derived feature rankings with the statistically significant features
from univariate tests to obtain a robust biomarker set with both predictive power and biological interpretability.
}
\examples{
set.seed(123)
x <- data.frame(rbind(t(rmultinom(30, 300, c(.21,.6,.12,.38,.099))),
                      t(rmultinom(30, 300, c(.001,.6,.42,.58,.299)))))
y <- factor(rep(c("Case", "Control"), each=30))
rf_model <- rf.cross.validation(x, y, nfolds=5)
bg_test <- BetweenGroup.test(x, y, clr_transform=TRUE, p.adj.method="fdr", q_cutoff=0.05)
id_robust_markers(rf_model, bg_test, top_n=3)
}
